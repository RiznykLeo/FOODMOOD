<%= simple_form_for current_list, html: { class: "d-flex" } do |f| %>
  <%= f.input :completed, as: :hidden, input_html: { value: "true" } %>
  <button type="submit" class="button active-red archive-button">
    <span class="button-text">Clear</span>
  </button>
<% end %>
